import SwiftUI

@main
struct TabViewApp: App {
    var body: some Scene {
        Window("Tab 页面实现", id: "main") {
            ContentView()
        }
        .defaultSize(width: 500, height: 400)
    }
}
